GEMINI_API_KEY=your_gemini_api_key_here
JWT_SECRET_KEY=change-this-in-production
JWT_EXPIRE_MINUTES=480
DATABASE_URL=sqlite:///./data/ethio_vitality.db

# Set true when Proteus hardware bridge is active (disables built-in simulator)
USE_HARDWARE=false

# JWT from login (user@ethio.dev) for simulation_bridge.py
BRIDGE_TOKEN=
INGEST_URL=import.meta.env.VITE_API_URL
